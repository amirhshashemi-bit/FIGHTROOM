function showToast(msg){const t=document.getElementById('toast');t.textContent=msg;t.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>t.classList.remove('show'),2800)}
async function loadFighterShowcase(){try{const r=await fetch('data/fighter_showcase.json',{cache:'no-store'});if(!r.ok)return;const d=await r.json();const root=document.getElementById('fighter-showcase-grid');if(!root)return;root.innerHTML=d.fighters.map((f,i)=>`<article class="showcase-card ${f.type}"><div class="showcase-top"><span>${String(i+1).padStart(2,'0')} • ${f.badge}</span><b>${f.record}</b></div><div class="showcase-visual"><span class="showcase-initials">${f.fa_name.split(' ').map(x=>x[0]).join('').slice(0,3)}</span><span class="showcase-division">${f.division}</span></div><div class="showcase-body"><small>${f.origin} • ${f.country}</small><h3>${f.fa_name}</h3><div class="showcase-en">${f.name}${f.nickname?' • '+f.nickname:''}</div><p>${f.story}</p><div class="showcase-stat">${f.stat}</div><div class="showcase-foot"><span>✓ ${f.verification}</span><a href="${f.source}" target="_blank" rel="noopener">${f.source_label} ↗</a></div></div></article>`).join('');document.getElementById('showcase-meta').textContent='ویترین اولیه آماده است: ۶ مبارز با پیوند مستند به ایران + ۲ چهره خارجی منتخب. این بخش برای بازبینی انسانی طراحی شده است.';}catch(e){}}
async function loadSiteStatus(){try{const r=await fetch('admin/status.json',{cache:'no-store'});if(!r.ok)return;const s=await r.json();if(s.maintenance){document.querySelectorAll('.featured,.packs,.dark').forEach(el=>el.hidden=true);showToast('سرویس موقتاً در حالت نگهداری است')}else if(s.premiumEnabled===false){document.querySelectorAll('.featured .buy,.packs .hot').forEach(el=>el.hidden=true)}}catch(e){}}
async function loadCatalog(){try{const r=await fetch('data/catalog.json',{cache:'no-store'});if(!r.ok)return;const d=await r.json();document.documentElement.dataset.catalogVersion=d.version||'';}catch(e){}}\ndocument.addEventListener('DOMContentLoaded',()=>{loadSiteStatus();loadCatalog();loadFighterShowcase();});
document.addEventListener('contextmenu',e=>{if(e.target.closest('.featured,.fighter-card,.hero-card'))e.preventDefault()});
document.addEventListener('dragstart',e=>{if(e.target.closest('img,.featured,.fighter-card,.hero-card'))e.preventDefault()});
document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&['s','u'].includes(e.key.toLowerCase()))e.preventDefault();if(e.key==='F12')e.preventDefault()});
